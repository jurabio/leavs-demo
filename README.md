### LeaVS demo

Usage:
1. Install the dependencies in a fresh python environment (tested with Python 3.11): `pip install -r requirements.txt`

2. Run the `leavs.ipynb` notebook. This will generate a synthetic dataset, train a
   simple model using the LeaVS objective, and evaluate the results.